{
  "name": "tsetlin-market-lab",
  "build": { "dockerfile": "../Dockerfile" },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "charliermarsh.ruff", "ms-toolsai.jupyter"]
    }
  },
  "postCreateCommand": "pip install -e '.[dev]' && (pip install tmu || true)",
  "remoteEnv": {
    "ODDS_API_KEY": "${localEnv:ODDS_API_KEY}"
  }
}
